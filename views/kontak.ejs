<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Kontak Foto Boy</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body style="font-family: 'Inter', sans-serif">
    <%- include('components/navbar') %>

    <div class="container mt-5">
      <div class="row min-vh-100 justify-content-center align-items-center">
        <div class="col-lg-8 col-md-10 col-12 rounded p-4 shadow">
          <h2 class="fw-bold mb-4 text-warning fw-bold">Kontak Kami</h2>
          <p>
            Silakan isi form di bawah untuk menghubungi tim Foto Boy. Kami akan membalas pesan Anda
            sesegera mungkin.
          </p>

          <form id="whatsappForm">
            <div class="mb-3">
              <label for="name" class="form-label fw-medium">Nama</label>
              <input
                type="text"
                class="form-control"
                id="name"
                name="name"
                placeholder="Nama lengkap"
                required
              />
            </div>
            <div class="mb-3">
              <label for="email" class="form-label fw-medium">Email</label>
              <input
                type="email"
                class="form-control"
                id="email"
                name="email"
                placeholder="Email aktif"
                required
              />
            </div>
            <div class="mb-3">
              <label for="message" class="form-label fw-medium">Pesan</label>
              <textarea
                class="form-control"
                id="message"
                name="message"
                rows="5"
                placeholder="Tulis pesan Anda di sini..."
                required
              ></textarea>
            </div>
            <button type="submit" class="btn btn-warning px-4">Kirim Pesan</button>
          </form>

          <script>
            const whatsappForm = document.getElementById('whatsappForm');
            whatsappForm.addEventListener('submit', function (e) {
              e.preventDefault();

              const name = document.getElementById('name').value.trim();
              const email = document.getElementById('email').value.trim();
              const message = document.getElementById('message').value.trim();

              // Format pesan
              const text = `Halo Foto Boy!%0ANama: ${name}%0AEmail: ${email}%0APesan: ${message}`;

              // Nomor WA (gunakan kode negara tanpa 0 depan: +6287817638249)
              const waNumber = '6287817638249';

              // Buka WA di tab baru
              window.open(`https://wa.me/${waNumber}?text=${text}`, '_blank');
            });
          </script>
        </div>
      </div>
    </div>

    <%- include('components/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
